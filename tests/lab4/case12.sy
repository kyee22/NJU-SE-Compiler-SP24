int main() {
    // 常量表达式的示例，运算包括加法、减法、乘法、除法、取模以及单目运算符

    // 例 1: 带有多种运算符的复杂表达式
    return
        +((10 + 20) * 3 - 5) / 4
        % 7
        +
        -(8 + 16) * 2
        /
        +15
        % 4
        * 3
        - !1
        + 0
        - 7;

    // 上面的表达式等于
    // 计算过程如下:
    // +((10 + 20) * 3 - 5) / 4 % 7
    // = +(30 * 3 - 5) / 4 % 7
    // = +(90 - 5) / 4 % 7
    // = +85 / 4 % 7
    // = 21 % 7
    // = 0

    // 计算过程：
    // - (8 + 16) * 2 / +15 % 4 * 3 - !1 + 0 - 7
    // = - (24) * 2 / 15 % 4 * 3 - !1 + 0 - 7
    // = -48 / 15 % 4 * 3 - !1 + 0 - 7
    // = -3 % 4 * 3 - !1 + 0 - 7
    // = 1 * 3 - !1 + 0 - 7
    // = 3 - !1 + 0 - 7
    // = 3 - 0 + 0 - 7
    // = -4

    // 结果会被计算并且 LLVM IR 会将其替换为最终值
}
